package org.outerj.pollo.xmleditor;

public interface Disposable
{
    /**
     * In the dispose method, an object should release any
     * resources it uses, as well any event listeners it has
     * registered.
     */
    public void dispose();
}
